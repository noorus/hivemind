#pragma once

#include <cstdint>
#include <vector>
#include <list>
#include <map>
#include <string>
#include <set>

namespace hivemind {

  using std::string;
  using std::stringstream;
  using std::vector;
  using std::list;
  using std::set;

  using GameTime = uint32_t;
  using RealTime = uint32_t;
  using PlayerID = uint32_t;
  using Real = float;

}